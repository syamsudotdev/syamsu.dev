# fly.toml app configuration file generated for react-router-ssg on 2025-12-02T22:33:06+07:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'react-router-ssg'
primary_region = 'sin'

[build]
  dockerfile = "Dockerfile.pnpm"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = 'suspend'
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
